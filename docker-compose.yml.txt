services:
  n8n:
    image: n8nio/n8n:latest
    container_name: n8n
    restart: always
    ports:
      - "5678:5678"
    volumes:
      - ~/n8n-data:/home/node/n8n
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=raspberrypiusername
      - N8N_BASIC_AUTH_PASSWORD=raspberrypipassword
      - N8N_TELEMETRY_DISABLED=true
      - N8N_HOST=raspberrypi IPaddress
      - N8N_SECURE_COOKIE=false